"""
Learning — library module, NOT an HTTP service.

This package is imported by other services. It does not run an HTTP
server, has no Dockerfile, and is not started by docker-compose. Do
not add `main.py` here.
"""
