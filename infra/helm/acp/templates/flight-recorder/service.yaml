{{- if .Values.flightRecorder.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "acp.fullname" . }}-flight-recorder
  namespace: {{ include "acp.namespace" . }}
  labels:
    {{- include "acp.componentLabels" (dict "component" "flight-recorder" "ctx" $) | nindent 4 }}
  {{- with .Values.global.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.flightRecorder.service.type }}
  selector:
    {{- include "acp.componentSelectorLabels" (dict "component" "flight-recorder" "ctx" $) | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.flightRecorder.service.port }}
      targetPort: {{ .Values.flightRecorder.service.targetPort }}
{{- end }}
