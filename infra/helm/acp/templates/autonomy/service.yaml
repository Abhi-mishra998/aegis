{{- if .Values.autonomy.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "acp.fullname" . }}-autonomy
  namespace: {{ include "acp.namespace" . }}
  labels:
    {{- include "acp.componentLabels" (dict "component" "autonomy" "ctx" $) | nindent 4 }}
  {{- with .Values.global.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.autonomy.service.type }}
  selector:
    {{- include "acp.componentSelectorLabels" (dict "component" "autonomy" "ctx" $) | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.autonomy.service.port }}
      targetPort: {{ .Values.autonomy.service.targetPort }}
{{- end }}
