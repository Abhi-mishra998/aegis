{{- if .Values.forensics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "acp.fullname" . }}-forensics
  namespace: {{ include "acp.namespace" . }}
  labels:
    {{- include "acp.componentLabels" (dict "component" "forensics" "ctx" $) | nindent 4 }}
  {{- with .Values.global.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.forensics.service.type }}
  selector:
    {{- include "acp.componentSelectorLabels" (dict "component" "forensics" "ctx" $) | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.forensics.service.port }}
      targetPort: {{ .Values.forensics.service.targetPort }}
{{- end }}
