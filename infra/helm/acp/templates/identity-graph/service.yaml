{{- if .Values.identityGraph.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "acp.fullname" . }}-identity-graph
  namespace: {{ include "acp.namespace" . }}
  labels:
    {{- include "acp.componentLabels" (dict "component" "identity-graph" "ctx" $) | nindent 4 }}
  {{- with .Values.global.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.identityGraph.service.type }}
  selector:
    {{- include "acp.componentSelectorLabels" (dict "component" "identity-graph" "ctx" $) | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.identityGraph.service.port }}
      targetPort: {{ .Values.identityGraph.service.targetPort }}
{{- end }}
